import { Login } from "@/components/Login";
import React from "react";

const page = () => {
  return (
    <div className="flex h-screen w-full">
      <Login />
    </div>
  );
};

export default page;
